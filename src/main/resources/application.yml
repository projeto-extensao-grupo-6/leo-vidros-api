server:
  servlet:
    context-path: /api
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  port: ${SERVER_PORT:8080}
  error:
    include-message: never
    include-binding-errors: never
    include-stacktrace: never
    include-exception: false
  forward-headers-strategy: framework

spring:
  application:
    name: extension-project-api

  config:
    import: optional:file:env[.properties],optional:file:.env[.properties]

  datasource:
    url: jdbc:mysql://localhost:3306/leo_vidros
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: false

  docker:
    compose:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      jdbc:
        time_zone: UTC

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:leo_vidros}
    password: ${RABBITMQ_PASS:leo_vidros_pass}
    template:
      retry:
        enabled: true
        initial-interval: 1000ms
        max-attempts: 3

jwt:
  secret: ${JWT_SECRET:minha_chave_secreta_padrao_para_dev_123}

app:
  environment: ${APP_ENV:development}
  pdf:
    storage-path: ${PDF_STORAGE_PATH:resources/pdfs}
  orcamento:
    diretorio: ${PDF_STORAGE_PATH:./storage/orcamentos}
  microservice:
    orcamento:
      url: ${MICROSERVICE_ORCAMENTO_URL:http://localhost:8082}

aws:
  region: ${AWS_REGION:us-east-1}
  access-key-id: ${aws_access_key_id}
  secret-access-key: ${aws_secret_access_key}
  session-token: ${aws_session_token}
  s3:
    bucket: ${AWS_S3_BUCKET}
